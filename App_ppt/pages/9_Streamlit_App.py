import streamlit as st

st.title('Streamlit Application')

st.markdown('''
    - We created one application where we classify the face images into real and fake.
    - Incorporated multiple models trained on different datasets.
    - Here's the demo of it: [FaceAuth]()
''')